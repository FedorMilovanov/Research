# Координация агентов: параллельная работа по 1 Кор. 11

**Статус:** `COORDINATION / MULTI-AGENT / CONFLICT-PROTECTION / HOLD`

> Указание владельца: агенты могут работать с теми же промтами по Коринфянам 11. Этот файл — место синхронизации, чтобы параллельные исследования не дублировались, не конфликтовали и не теряли друг друга.

---

## 1. Текущее состояние серии (этот агент / эта ветка)

- Ветка: `arena/019fe62d-research` (research-серия «1 Коринфянам 11»).
- Папка: `СЕРИЯ 1 КОРИНФЯНАМ 11/`.
- Стадия: `FOUNDATION`. НЕ публикуется, НЕ пушится на сайт.
- Файлы: `00` README/индекс → `05` очередь книг; `06` интеграция корпуса «Бытие 6»; `07` публикационный формат-ориентир; `08` этот файл.
- Машинный манифест: `data/1cor11-authority-manifest.json`.

---

## 2. Что уже сделано (не дублировать)
- Греческий текст 1 Кор. 11:2–16 (NA28-тип) и текстология — `01_...`.
- Исторический контекст Коринфа и покрытия головы — `02_...`.
- Карта cruces (κεφαλή, покрытие, ἐξουσία, ангелы, образ/слава, природа, применимость) — `03_...`.
- Ландшафт комментаторов (консервативные приоритетно) — `04_...`.
- Очередь приобретения полных книг — `05_...`.
- **Уже существующий глубокий анализ 1 Кор. 11:10** в `ТРУДНЫЕ ТЕКСТЫ` (греческий, патристика, адверсариальная проверка) — `06_...`.

---

## 3. Правила для параллельных агентов
1. **Перед новой работой** — прочитать `00_README_AND_MASTER_AUTHORITY_INDEX.md` и `06_PARALLEL_GENESIS6_CORPUS_INTEGRATION.md`.
2. **Не дублировать** существующий греческий разбор, патристику, адверсариальную проверку 11:10 — они уже в корпусе.
3. **Не переписывать чужие выводы без проверки** по исходному файлу/странице.
4. **Метки статуса**: любой новый тезис маркировать `PROVISIONAL` и `EVIDENCE_HOLD` до полного чтения книги.
5. **Конфликты**: если два агента предлагают разные прочтения одного узла — фиксировать обе позиции в `03_EXEGETICAL_CRUXES_MAP.md` с уровнями уверенности, не удаляя альтернативу молча (правило `HISTORICAL/CONFLICT`).
6. **Не открывать** site-статьи, пока не пройден publication-gate и согласование владельца.
7. **Единая точка правды** — файлы этой папки + манифест в `data/`; чатовые выжимки — не authority.

---

## 4. Как добавить свою работу
- Новые исследования → отдельный номерованный файл `NN_*.md` в папке серии.
- Обновить `00_README` карту владельцев и манифест `data/1cor11-authority-manifest.json`.
- Если нашли новую книгу/источник → добавить в `05_SOURCE_ACQUISITION_QUEUE_BOOKS.md`.
- Перед коммитом: `git status` / `git pull --rebase` / проверить, что не трогаете чужую активную ветку.

---

## 5. Смежные ветки / репозитории (проверено)
- **Research repo:** только `main` + текущая `arena/019fe62d-research`; отдельных веток по 1 Кор. 11 не обнаружено.
- **Сайт (gb-is-my-strength):** параллельные наработки по 1 Кор 11:10 живут не в ветках, а в `ТРУДНЫЕ ТЕКСТЫ` research-корпуса (серия «Бытие 6»). Учтено в `06_...`.
- **Параллельные промты агентов:** ожидаемы; этот файл — их точка входа.

---

## 6. Следующие шаги (очередь)
1. Приобрести и полностью прочитать консервативные комментарии (Ciampa–Rosner, Thiselton, Blomberg, Garland) — `05`.
2. Верифицировать греческий текст по печатному NA28/ECM2.
3. Построить claim-by-claim матрицу с уровнями уверенности.
4. Только после research-closure — publication-гейт.
