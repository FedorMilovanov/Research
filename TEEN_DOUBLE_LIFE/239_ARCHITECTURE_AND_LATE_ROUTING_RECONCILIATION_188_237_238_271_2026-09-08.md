# Architecture + late-routing reconciliation — `188` / `237` and `238` / `271`

**Date:** 2026-09-08  
**Mode:** RESEARCH-ONLY / RECONCILIATION / NOT PRODUCT APPROVAL  
**Series:** `TEEN_DOUBLE_LIFE`

---

## 1. Why this file exists

Parallel research produced two apparent architecture pairs that could be misread as conflicts:

- `188_EXPANDED_SERIES_ARCHITECTURE...` vs `237_PUBLICATION_ARCHITECTURE_REVIEW...`;
- `238_LATE_ROUTING_SHAME_ANGER_FAMILY_SECRETS_SIBLING_WITNESS...` vs `271_LATE_ROUTING_266_270...`.

They are not equivalent conflicts. This file fixes ownership so a future agent does not silently discard one research lane or use an older partial routing as current authority.

---

## 2. `188` vs `237` — complementary companion rings, not mutual superseders

### `188` owns the specialized gap-audit companion ring

`188` arose from the broad 2026-09-08 gap audit and assigns specialized topics such as:

- positive sexual formation;
- child-on-child sexual harm;
- scrupulosity/tender conscience;
- assurance/shame after repeated sexual sin;
- secret romance/cyber dating abuse;
- institutional/church integrity;
- fractured/two-home/sibling ecology;
- masturbation/conscience;
- synthetic/purchased intimacy / deepfake / AI.

Its core function is:

> **specialized-topic coverage after identifying broad gaps not fully owned by the original trilogy.**

### `237` owns the later owner-requested adult/family-government ring

`237` arose from later explicit owner requirements about:

- adult child departure/rebellion/contact/return;
- adult co-residence, house rules, money and consequences;
- adulthood and continuing parental authority;
- daughter/father/courtship/marriage/family-government.

Its core function is:

> **claim ownership for the large adult/prodigal/family-government corpus that no longer fits one generic adult companion.**

### Therefore

`188 ≠ SUPERSEDED_BY_237`.

`237 ≠ SUPERSEDED_BY_188`.

They are **parallel companion rings around the same locked core trilogy**.

The core remains:

1. Part I — Hidden life;
2. Part II — Parents;
3. Part III — Church.

### Publication guard

Neither `188` nor `237` authorizes opening all proposed companions.

Research architecture coverage ≠ Product roadmap commitment.

Default sequencing remains core first unless the owner explicitly reprioritizes. Every new Product companion requires live Product collision/ownership/source/CI preflight.

### Editorial economy guard

Where a specialized `188` topic overlaps a `237` companion, use cross-link/section ownership rather than duplicated full exegesis.

Examples:

- secret romance (`188 C5`) may cross-link daughter/courtship (`237 Companion D`) but does not become the same article;
- sibling risk (`188 C7`) may cross-link house/resources (`237 Companion B`) where younger children need protection;
- institutional integrity (`188 C6`) belongs primarily church-side and must not be swallowed by adult-family government;
- assurance/scrupulosity (`188 C3/C4`) remains soteriological/pastoral, not an adult-jurisdiction appendix.

---

## 3. `238` vs `271` — later routing supersedes partial routing

`238` was created after direct review of `266–269` and correctly routed:

- shame/public humiliation;
- parental anger/intimidation;
- family secrecy/privacy;
- sibling witness vs spying.

A parallel agent then added `270_ACCOUNTABILITY_TRANSPARENT_OVERSIGHT...` and created `271`, which owns the complete range `266–270` and explicitly integrates `237` architecture.

### Ownership decision

`238 = HISTORICAL_PARTIAL_ROUTING / STILL_VALID_FOR_266–269_DETAILS`.

`271 = CURRENT_ROUTING_OWNER_FOR_266–270`.

A future agent should use `271` as the entry routing file and consult `238` only where its compression or exact formulation is useful.

Do not delete `238`; no factual contradiction requires history rewriting.

---

## 4. Current late-routing chain

For late parenting/core overlays use:

`224[245/246/249/250]`
→ `230[251–255]`
→ `234[256–258]`
→ `265[259–264]`
→ `271[266–270]`.

`235` remains the reconciliation control for `234` vs `265` overlap.

`239` is the architecture/routing reconciliation control for `188/237` and `238/271`.

### Rule

> **NEW RESEARCH MODULE ≠ NEW PRODUCT ARTICLE.**

Parallel additions must first be routed into an existing claim owner unless they genuinely establish a new primary pastoral question that survives architecture review.

---

## 5. Mandatory P0s preserved from `238/271`

Even when compressed, preserve:

- `DISCIPLINE ≠ HUMILIATION`;
- `CONFESSION ≠ PUBLICATION`;
- `AUTHORITY ≠ INTIMIDATION`;
- `REVERENCE ≠ PANIC`;
- `PRIVACY ≠ DECEPTION`;
- `CONFIDENTIALITY ≠ DANGEROUS SECRECY`;
- `LOYALTY ≠ CONCEALMENT OF DANGER`;
- `WITNESS ≠ SPY`;
- `REPORTING DANGER ≠ TATTLING`;
- `ACCOUNTABILITY ≠ SURVEILLANCE`;
- `TEMPORARY INSPECTION ≠ PERMANENT STIGMA`;
- parent-led minor oversight ≠ universal adult parental data access;
- voluntary truth/help-seeking remains pastorally meaningful without proving regeneration.

---

## 6. Final reconciliation state

- `CORE_TRILOGY = ONE`.
- `188_SPECIALIZED_GAP_COMPANION_RING = PRESERVED`.
- `237_ADULT_FAMILY_GOVERNMENT_COMPANION_RING = PRESERVED`.
- `188/237 = COMPLEMENTARY, NOT COMPETING`.
- `271 = CURRENT_ROUTING_OWNER_FOR_266–270`.
- `238 = EARLIER_PARTIAL_ROUTING, NOT CURRENT OWNER`.
- `NEW_PRODUCT_ARTICLE = REQUIRES_NEW_LIVE_PREFLIGHT`.
